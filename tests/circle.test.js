const Circle = require('../lib/Shapes.js');

//testing suite for Circle is created
describe('Circle', () => {
    //A test is created to check that a Circle is created when Circle is chosen
    describe('Circle', () => {
        it('should create a logo.svg', () => {
            const Circle = 'Circle';
            expect(generateLogo()).toEqual('Circle');           
        });
    });
});